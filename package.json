{
  "name": "@deployable/test-cli",
  "version": "0.1.0",
  "description": "Cli command and code test helpers",
  "main": "lib/index.js",
  "scripts": {
    "test": "set -uex; yarn run test:unit; yarn run test:int",
    "test:unit": "mocha test/unit*",
    "test:int": "mocha test/int*",
    "test:debug": "DEBUG='*' mocha debug",
    "test:coverage": "istanbul cover _mocha && istanbul check-coverage",
    "release:test": "set -uex; yarn test; npm version prerelease; yarn publish --tag testing --access public",
    "release:patch": "set -uex; yarn run test:coverage; npm version patch; git push; yarn publish --access public; git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deployable/node-deployable-test-cli.git"
  },
  "keywords": [
    "test",
    "cli",
    "deployable",
    "module"
  ],
  "author": "Matt Hoyle <code at deployable.co>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/deployable/node-deployable-test-cli/issues"
  },
  "homepage": "https://github.com/deployable/deployable-test-cli#readme",
  "devDependencies": {
    "@deployable/test": "^0.0.5",
    "chai": "^3.5.0",
    "istanbul": "~0.4.5",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "debug": "^2.6.2"
  }
}
